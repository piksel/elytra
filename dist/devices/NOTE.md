## /devices path

This path can be used to add assets for "known" devices.
If you want a device added here, just make a PR.

Since the core idea of the library is that devices should not need to be pre-registered to be configured, 
adding a device is **purely optional**.